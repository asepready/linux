# Catatan paket (Debian 12 / panduan Graylog Core) — bukan skrip instalasi otomatis
#
# MongoDB (repo mongodb-org 8.0, lihat README induk):
#   mongodb-org
#
# Repositori Graylog (paket .deb dari packages.graylog2.org), lalu:
#   graylog-datanode    # node Data Node
#   graylog-server      # node Graylog + MongoDB (Graylog Open)
#
# Edisi berbayar memakai paket berbeda; lihat dokumentasi resmi Graylog.
